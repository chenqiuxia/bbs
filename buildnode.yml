version: 0.2

phases:
  #install:
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      - npm install
      - node build/build.js
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
    - buildspec.yml
    - Dockerfile
    - nginx.conf
    - dist/**/*
    - appspec.yml
    - scripts/*
